# Copy to .env. Required values intentionally blank so startup fails closed.
# Set NINEROUTER_IMAGE to khasvn/9router@sha256:<tested manifest digest>.
NINEROUTER_IMAGE=
NINEROUTER_PORT=20128
INITIAL_PASSWORD=
JWT_SECRET=
API_KEY_SECRET=
MACHINE_ID_SALT=
# Set true when dashboard is reached exclusively through HTTPS.
AUTH_COOKIE_SECURE=false
